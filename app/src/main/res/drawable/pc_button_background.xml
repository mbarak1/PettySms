<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight"> <!-- Adjust the ripple color as needed -->

    <!-- Your custom background shape -->
    <item android:id="@android:id/background">
        <shape xmlns:android="http://schemas.android.com/apk/res/android"
            android:shape="rectangle">
            <corners android:radius="15dp" /> <!-- Adjust the corner radius as needed -->
            <solid android:color="@android:color/white" /> <!-- Adjust the background color as needed -->
        </shape>
    </item>

    <!-- Ripple mask -->
    <item android:id="@android:id/mask">
        <shape xmlns:android="http://schemas.android.com/apk/res/android"
            android:shape="rectangle">
            <corners android:radius="15dp" /> <!-- Adjust the corner radius as needed -->
            <solid android:color="@android:color/white" /> <!-- Adjust the mask color as needed -->
        </shape>
    </item>
</ripple>